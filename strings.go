package main

import (
  "fmt"
  "strings"
)



func main() {
var teststr string = "This is simply a test string to print with mf"
fmt.printf("t/f?" does the string \"%s\" has prefix %s? ", teststr", Th")
fmt.printf("%t\n"", strings.hasprefix(str, "th"))
s := "Some"+"Thing"
fmt.Println(s)
}
